<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/Header') %>
    <body>
        <%- include('./partials/Navbar') %>
        <div class="container py-4 mt-2 bookList">
            <h3>My Book List</h3>
            <div class="container row px-0">
                <% if (myList.length > 0) { %>
                    <% myList.forEach(list => { %>
                            <div class="list p-4 my-3 mb-3 col-md-3">
                                <h3>Title: <%= list.title %></h3>
                                <h4>Author: <%= list.author %></h4> 
                                <div>Did you read: 
                                    <% if (list.isRead) { %>
                                        Yes
                                    <% } else { %>
                                        No
                                    <% } %>
                                </div>
                                <div class="mt-3">
                                    <a href="/booklist/<%= list._id %>" class="btn btn-success">Read More</a>
                                </div>
                            </div>
                    <% }) %>
                <% } else { %>
                    <div class="text-danger">
                        <h3>You have no book in your list</h3>
                    </div>
                <% } %>
            </div>
        </div>
        <%- include('./partials/Footer') %>
    </body>
</html>